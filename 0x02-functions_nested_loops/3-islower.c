#include "main.h"

/**
 * _islower - Entry point
 *
 * Return: 0 Always (success)
 */

int _islower(int c)
{
	int result;

	if ("%d" >= 97 && "%d" <= 125)
		result = 1;
	else
		result = 0;
	return (result);
}
