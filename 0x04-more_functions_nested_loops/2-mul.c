#include "main.h"

/**
 * mul - Entry point to the function for multiplying two numbers
 *
 * @a: first number
 * @b: second number
 *
 * Return: 0 Always (success)
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
