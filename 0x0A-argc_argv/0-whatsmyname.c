#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * @argc: variable count
 * @argv: arguments
 *
 * Return: The name of the program
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
