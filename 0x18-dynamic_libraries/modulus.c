#include "main.h"
#include <stdio.h>

/**
 * mod - function for finding the modulo of two numbers
 * @a: fist operand
 * @b: second operand
 * Return: Always int
 */

int mod(int a, int b)
{
	return (a % b);
}
