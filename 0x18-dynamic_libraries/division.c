#include "main.h"
#include <stdio.h>

/**
 * div - function for the division of two numbers
 * @a: fist operand
 * @b: second operand
 * Return: Always int
 */

int div(int a, int b)
{
	return (a / b);
}
