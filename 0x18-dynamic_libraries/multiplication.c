#include "main.h"
#include <stdio.h>

/**
 * mul - function for multiplying two numbers
 * @a: fist operand
 * @b: second operand
 * Return: Always int
 */

int mul(int a, int b)
{
	return (a * b);
}
